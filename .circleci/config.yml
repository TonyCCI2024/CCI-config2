version: 2.1

jobs:
  hello-world:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Print Hello World
          command: echo "Hello, World!"

workflows:
  version: 2
  hello_world_workflow:
    jobs:
      - hello-world
